# Audit Logs UI

The Audit Logs workspace refreshes every ten seconds and supports filtering records by event type or searching by order ID and event type. It labels the event source so operators can distinguish durable PostgreSQL history from the local runtime fallback.
