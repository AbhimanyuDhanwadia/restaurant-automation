# Audit Logs UI

The Audit Logs workspace refreshes every ten seconds and supports filtering operational records by event type or searching by order ID and event type. It labels the event source so operators can distinguish durable PostgreSQL history from the local runtime fallback. A separate access-assignment panel shows the actor, target identity, prior role, new role, and timestamp for each role change.
