# Settings UI

The Settings page reads and saves its form through the authenticated settings API. It supports restaurant identity, timezone, operational alerts, and the stored auto-advance preference.

The page reports loading and request failures, disables controls while the initial configuration is loading, and refreshes its cached configuration after a successful save.
