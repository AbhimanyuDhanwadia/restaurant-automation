# Staff UI API Connection

The Staff workspace uses the authenticated Staff API for roster data, status changes, per-member handoffs, the manager shift handoff, and open shift tasks. The page no longer reads or writes local mock staff state.

The Live Operations task panel still uses its legacy local module and will be connected to this durable task feed in a dedicated dashboard milestone.
