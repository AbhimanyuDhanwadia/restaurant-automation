# Database UI

The Database workspace refreshes read-only metadata every 15 seconds. It shows database configuration state, migration count, latest applied version, and the migration ledger without exposing sensitive connection details or query controls.
